

services:

  web:
    image: flask-redis-web:latest
    container_name: flask-web
    ports:
      - "5000:5000"

    depends_on:
      - redis

  redis:
    image: redis:latest
    container_name: redis-server
