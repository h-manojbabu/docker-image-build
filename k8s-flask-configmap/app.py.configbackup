from flask import Flask
import os

app = Flask(__name__)

@app.route('/')
def home():

    app_name = os.getenv("APP_NAME", "Default App")

    return f"""
    <h1>{app_name}</h1>
    <h2>ConfigMap Demo</h2>
    """

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)
